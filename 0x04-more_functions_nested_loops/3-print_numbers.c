#include "main.h"
/**
 * print_numbers - print the numbers fro 0 to 9
 * Return: always 0
 */
void print_numbers(void)
{
	int i;

	for (i = 0; i <= 9; i++)
	{
		printf("%d", i);
	}

	printf("\n");

	return (0);
}
